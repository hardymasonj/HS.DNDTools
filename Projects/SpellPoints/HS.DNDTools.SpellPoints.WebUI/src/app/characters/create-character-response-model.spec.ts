import { CreateCharacterResponseModel } from './create-character-response-model';

describe('CreateCharacterResponseModel', () => {
  it('should create an instance', () => {
    expect(new CreateCharacterResponseModel()).toBeTruthy();
  });
});
