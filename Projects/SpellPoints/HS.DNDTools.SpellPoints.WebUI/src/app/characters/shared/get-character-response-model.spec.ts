import { GetCharacterResponseModel } from './get-character-response-model';

describe('GetCharacterResponseModel', () => {
  it('should create an instance', () => {
    expect(new GetCharacterResponseModel()).toBeTruthy();
  });
});
