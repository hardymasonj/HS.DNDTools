import { CastSpellResponseModel } from './cast-spell-response-model';

describe('CastSpellResponseModel', () => {
  it('should create an instance', () => {
    expect(new CastSpellResponseModel()).toBeTruthy();
  });
});
